# 運用手順(毎日のワークフロー)

## 仕組みの全体像

```
毎朝6時(自動)                  あなた(1記事あたり5〜10分)
┌─────────────────┐   ┌──────────────────────────────┐
│ Coworkが記事を1本生成      │   │ 1. 通知を確認                       │
│ ・テーマ/収益化方式を判断  │ → │ 2. 記事をコピペしてnoteに投稿       │
│ ・執筆→7軸評価→リライト   │   │ 3. X告知文を貼ってツイート          │
│ ・GitHubへ自動プッシュ     │   │ 4.「投稿した」とCoworkに一言(任意) │
└─────────────────┘   └──────────────────────────────┘
```

- 記事生成は **Coworkの定期実行(ルーティン)** が毎朝6時に自動で行います。あなたが毎回お願いする必要はありません
- Claude APIは使わないため **追加費用ゼロ** です(お使いのClaudeプランの利用枠内で動きます)
- 完了すると **プッシュ通知とメール** が届き、記事タイトル・方式・価格・評価点が報告されます

## あなたが毎日やること

### ① 記事を受け取る(2通りどちらでも)

**A. GUIで見る(おすすめ)**: PCの `note-tool` フォルダで最新を取り込んでから起動:

```
cd %USERPROFILE%\note-tool
git pull
```

→ 起動.bat をダブルクリック → 「記事一覧」タブに今朝の記事が表示されます

※ 起動.bat をメモ帳で開き、`cd /d %~dp0` の次の行に `git pull` と1行足しておくと、ダブルクリックだけで常に最新になります。

**B. GitHubで直接見る**: https://github.com/YK-maker-09/-Automated-Article-Generation/tree/claude/note-article-generator-jz7mqt/articles → 今月のフォルダ → 記事フォルダ内の `article.md` と `review.md`

### ② noteに投稿する

1. `review.md`(最終チェックシート)で評価と改善余地をざっと確認
2. `article.md` の本文をコピーして、noteの新規記事に貼り付け
3. 有料記事の場合: 本文中の「＝＝＝＝＝ ここから有料ライン ＝＝＝＝＝」の位置に有料ラインを設定し、そのマーカー行を削除。価格は review.md 記載の金額に
4. review.md 記載のハッシュタグを付けて公開

### ③ Xで告知する

`review.md` の末尾にある「X告知文の下書き」をコピーし、末尾にnote記事のURLを付けてXに投稿するだけ。

X APIキーを設定済みなら、コマンドでも投稿できます(Claude APIキー不要):

```
python -m note_tool posted <記事ID> --url <noteのURL> --tweet "<告知文>"
```

### ④ 投稿を記録する(任意・推奨)

Coworkに「20260705-001 投稿した。URLは https://note.com/...」と伝えるだけで台帳に記録されます(月間の進捗・売上見込みの集計に使われます)。③のコマンドを使った場合は自動で記録済みです。

## 生成ペースの変更・停止

Coworkに話しかけるだけで変更できます:

- 「自動生成を止めて」/「再開して」
- 「週3本にして」「1日2本にして」
- 「明日はテーマ○○で作って」(その日だけの指定もOK)

## トラブル時

- **朝の記事が来ていない** → Coworkでこのプロジェクトを開き「今日の記事は?」と聞く(その場で生成させられます)
- **記事の質に不満** → review.md の改善点を添えて「この記事をリライトして」と言えば作り直します
- **GUIに記事が出ない** → `git pull` を忘れていないか確認

## 費用について

- 記事生成: **0円**(Coworkの定期実行。お使いのClaudeプランの利用枠内)
- X自動投稿: **0円**(X APIの無料枠)
- GUIの「生成スタート」ボタンだけはClaude API(有料)を使います。ルーティン運用では押す必要はありません
